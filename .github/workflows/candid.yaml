on:
  pull_request:
    branches:
      - master

name: Validate candid files

jobs:
  candid:
    name: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
        chmod +x ./v2/validate-candid.sh
        ./v2/validate-candid.sh
