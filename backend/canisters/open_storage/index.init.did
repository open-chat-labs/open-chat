type Version =
    record {
        major: nat32;
        minor: nat32;
        patch: nat32;
    };

type InitArgs =
    record {
        service_principals: vec principal;
        bucket_canister_wasm: record {
            version: Version;
            module: blob;
        };
        wasm_version: Version;
        test_mode: bool;
    };

service: (InitArgs) -> {
}
