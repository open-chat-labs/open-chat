[package]
name = "timer_job_queues"
version.workspace = true
edition.workspace = true

[dependencies]
futures = { workspace = true }
ic-cdk = { workspace = true }
per_round_timer = { path = "../per_round_timer" }
serde = { workspace = true }
types = { path = "../types" }
