# The build lives in a script so that CI can run the identical build to warm its cache; see the
# comments there for the flags it sets.
build_cmd:
  ./scripts/build-benchmarks-wasm.sh

wasm_path:
  ./target/wasm32-unknown-unknown/release/benchmarks.wasm
