{
  "name": "openchat-frontend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm i && turbo run build",
    "build:ci": "npm ci && turbo run build:ci",
    "deploy:prod_test": "npm ci && turbo run deploy:prod_test",
    "deploy:prod": "npm ci && turbo run deploy:prod",
    "deploy:testnet": "npm ci && turbo run deploy:testnet",
    "dev": "npm i && turbo run dev",
    "changelog": "auto-changelog --tag-pattern '-website' --file-pattern '^frontend'"
  },
  "author": "julian.jelfs@gmail.com",
  "workspaces": [
    "app",
    "openchat-client",
    "openchat-agent",
    "openchat-shared",
    "openchat-worker",
    "openchat-push",
    "openchat-sw"
  ],
  "devDependencies": {
    "auto-changelog": "open-chat-labs/auto-changelog#master",
    "turbo": "^1.10.12"
  }
}
