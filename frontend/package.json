{
    "name": "openchat-frontend",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "build": "npm i --prefer-offline && dotenv -- turbo run build --cache-dir .turbo",
        "build:ci": "npm ci --prefer-offline && dotenv -- turbo run build:ci --cache-dir .turbo",
        "build:prod": "npm ci --prefer-offline && dotenv -- turbo run build:prod --cache-dir .turbo",
        "build:prod_test": "npm ci --prefer-offline && dotenv -- turbo run build:prod_test --cache-dir .turbo",
        "build:testnet": "npm ci --prefer-offline && dotenv -- turbo run build:testnet --cache-dir .turbo",
        "deploy:prod_test": "npm ci --prefer-offline && dotenv -- turbo run deploy:prod_test --cache-dir .turbo",
        "deploy:prod": "npm ci --prefer-offline && dotenv -- turbo run deploy:prod --cache-dir .turbo",
        "deploy:testnet": "npm ci --prefer-offline && dotenv -- turbo run deploy:testnet --cache-dir .turbo",
        "dev": "npm i --prefer-offline && dotenv -- turbo run dev --cache-dir .turbo",
        "changelog": "auto-changelog --tag-pattern '-website' --file-pattern '^frontend'"
    },
    "author": "julian.jelfs@gmail.com",
    "workspaces": [
        "app",
        "openchat-agent",
        "openchat-client",
        "openchat-push",
        "openchat-shared",
        "openchat-worker"
    ],
    "devDependencies": {
        "@babel/core": "^7.22.10",
        "@babel/preset-env": "^7.22.10",
        "@typescript-eslint/eslint-plugin": "^6.4.0",
        "@typescript-eslint/parser": "^6.4.0",
        "eslint-config-prettier": "^9.0.0",
        "eslint-plugin-prettier": "^5.0.0",
        "auto-changelog": "open-chat-labs/auto-changelog#master",
        "babel-jest": "^29.6.2",
        "babel-loader": "^9.1.3",
        "dotenv-cli": "^7.2.1",
        "ts-jest": "^29.1.1",
        "turbo": "^1.10.13"
    }
}
