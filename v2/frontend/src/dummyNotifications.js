/**
 * This ridiculous thing is needed as the easiest way to keep jest happy.
 * Might need to add more dummy functions to this as time goes by.
 */
export function closeNotificationsForChat(_chatId) {
    return undefined;
}
